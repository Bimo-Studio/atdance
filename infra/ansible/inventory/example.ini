# Copy to production.ini (gitignored) or pass -i "IP," on the command line.
# Replace 203.0.113.10 with terraform output droplet_ipv4.

[prod]
atdance-prod ansible_host=203.0.113.10

[prod:vars]
ansible_user=root
ansible_ssh_private_key_file=~/.ssh/id_ed25519
# First-time only (then remove this line):
# ansible_ssh_common_args=-o StrictHostKeyChecking=accept-new
